language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-twoblokes-static' ]; then cd .. && eval "mv $currentfolder generator-twoblokes-static" && cd generator-twoblokes-static; fi

